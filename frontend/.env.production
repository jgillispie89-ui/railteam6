# Backend now runs on Vercel (was Render). This must match the API project's URL —
# either the custom domain below or the generated https://<project>.vercel.app URL.
# Netlify builds bake VITE_* at build time, so also set this in Netlify env + redeploy.
VITE_API_BASE=https://api.railteam6.com
